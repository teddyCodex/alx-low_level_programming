#include "main.h"

/**
 * _puts - function that prints a string, followed by a new line, to stdout
 * @str: string to be evaluated
 * Return: Always 0.
 */

void _puts(char *str)
{
	_puts("%s\n", str);
}
