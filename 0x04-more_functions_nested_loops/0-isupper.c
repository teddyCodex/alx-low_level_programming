#include "main.h"

/**
 * _isupper - Entry point
 * program to check for UPPERCASE character
 * Return: 1 (true) || 0 (false)
 */

int _isupper(int c)
{
	if (c >= 'A' || c >= 'Z')
	{
		return (1);
	}
	{
		return (0);
	}
}
