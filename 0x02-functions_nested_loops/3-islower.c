#include "main.h"

/**
 * _islower - Entry point
 * int c - value to check
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
	if (_islower(c))
	{
		_putchar('1');
		return (1);
	} else
	{
		_putchar('0');
		return (0);
	}
}
