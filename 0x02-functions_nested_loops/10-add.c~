#include "main.h"

/**
 * add - Entry point
 * @x: integers to be evaluated
 * @y: integer to be evaluated
 * function that adds two integers and returns the result
 * Return: Always 0 (Success)
 */
int add(int x, int y)
{
	int res;
	res = x + y;
	return res;
}
